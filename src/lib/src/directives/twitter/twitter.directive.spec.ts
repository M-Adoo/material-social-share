import { TwitterDirective } from './twitter.directive';

describe('TwitterDirective', () => {
  it('should create an instance', () => {
    const directive = new TwitterDirective();
    expect(directive).toBeTruthy();
  });
});
