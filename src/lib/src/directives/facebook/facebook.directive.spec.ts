import { FacebookDirective } from './facebook.directive';

describe('FacebookDirective', () => {
  it('should create an instance', () => {
    const directive = new FacebookDirective();
    expect(directive).toBeTruthy();
  });
});
