import { DoubanDirective } from './douban.directive';

describe('DoubanDirective', () => {
  it('should create an instance', () => {
    const directive = new DoubanDirective();
    expect(directive).toBeTruthy();
  });
});
