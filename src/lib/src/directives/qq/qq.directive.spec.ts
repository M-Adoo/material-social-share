import { QQDirective } from './qq.directive';

describe('QqDirective', () => {
  it('should create an instance', () => {
    const directive = new QQDirective();
    expect(directive).toBeTruthy();
  });
});
