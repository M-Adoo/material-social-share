import { QzoneDirective } from './qzone.directive';

describe('QzoneDirective', () => {
  it('should create an instance', () => {
    const directive = new QzoneDirective();
    expect(directive).toBeTruthy();
  });
});
