import { WeiboDirective } from './weibo.directive';

describe('WeiboDirective', () => {
  it('should create an instance', () => {
    const directive = new WeiboDirective();
    expect(directive).toBeTruthy();
  });
});
