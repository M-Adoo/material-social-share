import { LinkedinDirective } from './linkedin.directive';

describe('LinkedinDirective', () => {
  it('should create an instance', () => {
    const directive = new LinkedinDirective();
    expect(directive).toBeTruthy();
  });
});
