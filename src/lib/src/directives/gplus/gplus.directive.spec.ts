import { GplusDirective } from './gplus.directive';

describe('GplusDirective', () => {
  it('should create an instance', () => {
    const directive = new GplusDirective();
    expect(directive).toBeTruthy();
  });
});
