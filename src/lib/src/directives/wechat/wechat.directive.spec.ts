import { WechatDirective } from './wechat.directive';

describe('WechatDirective', () => {
  it('should create an instance', () => {
    const directive = new WechatDirective();
    expect(directive).toBeTruthy();
  });
});
